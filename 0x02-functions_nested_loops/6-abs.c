#include "main.h"
/**
 * _abs - computes the abolute value of a num
 * @n: the num in question
 *
 * Return: the absolute of the num
 */

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
